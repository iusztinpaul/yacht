syntax = "proto3";

package yacht.config.proto;

message MetaConfig {
  string device = 1;
  repeated string metrics_to_save_best_on = 2;
  repeated string metrics_to_load_best_on = 3;
  int32 plateau_max_n_steps = 4;
  int32 log_frequency_steps = 5;
  string experiment_tracker = 6;
}